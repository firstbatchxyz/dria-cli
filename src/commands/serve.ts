/**
 * Serve a given data via Dria HNSW. Assumes that the contract has been
 * pulled before-hand, via `dria pull <contract-id>`.
 *
 * @param contractId contract ID
 */
export async function cmdServe(contractId: string) {
  throw "unimplemented";
}
